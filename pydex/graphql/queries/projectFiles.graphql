query projectFiles ($projectId: ID!, $limit: Int!, $offset: Int!) {
    project(id: $projectId) {
        id
        projectFiles(limit: $limit, offset: $offset) {
            total
            items {
                size
                contentType
                downloadUrl
                localPath
                etag
            }
        }
    }
}
mutation archiveProject(
  $projectId: ID!
  $project: ProjectInput!
) {
  # DEPPRECATED: This is not how we archive projects going forward
  # updateProject(project: $project, projectId: $projectId) {
  #   id
  #   name
  # }
}
